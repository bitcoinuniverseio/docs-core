## What a reader can now do

<!-- One or two sentences. What could they not do before this change? -->

## Grounding

<!-- Where did the facts come from? Name the file, the endpoint, or the
     capture. If a live value is quoted, say when it was read. -->

- Source:
- Verified on:

## Checks

- [ ] `npm test` passes locally
- [ ] `npm run build` passes locally, so every internal link and anchor resolves
- [ ] No em dash, and not the word beginning with "canon"
- [ ] No capability is presented as available that is not released
- [ ] Any protocol page change came from `node scripts/generate-protocol-pages.mjs`, not a hand edit
- [ ] Any new image is a real capture, has alt text, and is under 200 KB
- [ ] Nothing private is published: no hostname, IP address, credential, or operator procedure
